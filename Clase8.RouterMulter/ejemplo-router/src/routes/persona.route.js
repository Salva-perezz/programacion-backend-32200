import { Router } from "express";

const router = Router();
const personas = [];

router
  .route("/")
  .get((req, res) => {})
  .post((req, res) => {})
  .put((req, res) => {})
  .delete((req, res) => {});

export default router;
