const number1: number = 2;
const number2: number = 2;

const sumar = (num1: number, num2: number): number => {
  return num1 + num2;
};

const result: number = sumar(number1, number2);

console.log(result);
