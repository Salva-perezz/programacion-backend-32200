paths:
  /number:
    post:
      summary: Add one number to the list
      tags:
        - Number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NumberRequestBody"
